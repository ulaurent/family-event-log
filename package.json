{
  "name": "baby-log-fe",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cf:build": "npm run build",
    "cf:deploy": "npm run build && npx wrangler pages deploy dist --project-name baby-log-fe",
    "cf:deploy:preview": "npm run build && npx wrangler pages deploy dist --project-name baby-log-fe --branch preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
